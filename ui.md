## 界面设计文档（修订版）

界面我准备设计成这样：
左边是题目区，右边是 Agent 对话区，最上面是菜单栏，最下面是状态栏。

界面主要由几种“组件”构成：
- Markdown 编辑器：用户可以编辑 Markdown 并实时预览，带有语法高亮。预览采用防抖策略，用户停止输入 500ms 后触发渲染，避免高频编辑造成卡顿。
- 文件编辑器：用户可以简单地编辑文件内容，或选择用本地编辑器打开文件编辑，带有语法高亮（对于体积过大的文件或二进制文件，提示用户，用户确认后可以打开）。前端文件编辑器会监听磁盘文件变更，当用户使用本地编辑器修改文件或 Agent 修改文件时，前端自动重新加载更新后的内容(如果 Web 端未保存则提示)，避免数据覆盖或显示过期。
- 下拉菜单：用户可以在一个列表中选择一项
- 表格：以表格形式显示一些信息，其中可能包含输入框、文件链接（显示文件名，点击打开文件编辑器）或下拉菜单，用于编辑配置文件等
- Spoiler：默认只有一个标题，点击旁边的箭头可以展开具体内容，再点击箭头还可以收回
- 对话框：用于显示用户与 agent 的对话

**布局约束**：题目区与对话区之间提供拖拽分割线（Resizable Splitter），用户可自由调整左右宽度比例，以适应题面长公式或代码块的阅读需求。

---

### 题目区

题目区上面是题目的选项卡（最右边一个+标签表示新建题目），下面是题目详情，题目详情部分只显示选中的题目。

题目详情部分有题目名称作为标题，有“单题自测”按钮，有“基本信息”“题面”“题解”“数据”“辅助程序”“解法”标签页，同样地下面也只显示选中的一个标签页。

- 基本信息：可以设置题目的名称、类型（传统/函数交互/IO交互/提交答案）、难度、标签、时间限制、空间限制
- 题面：（Markdown 编辑器，用户可以编辑并实时预览）
- 题解：（同样是 Markdown 编辑器，用户可以编辑并实时预览）
- 数据：题目的测试数据，列出每个子任务及其评测方式、依赖等，每个子任务是 Spoiler 的形式，点击可以展开具体测试点：以表格形式显示【测试点编号、输入文件、输出文件】（blob 模式）或【测试点编号、generator 命令行参数】（generated 模式）。此标签页主要供用户查看 Agent 生成的数据结果，编辑功能较少使用，不设计为从头创建数据的入口。
- 辅助程序：generator、validator、checker、std 及可能存在的交互库，分栏展示（交互库一栏列出 interactive_lib.cpp 和 <题目名称>.h 两个文件），点击文件可以用文件编辑器打开
- 解法：列出 std 及各种其他解法以及它们预期的评测结果、得分

根据 agent 数据模型中的组件状态：

```rust
pub enum ComponentStatus {
    #[default]
    NotStarted,
    InProgress {
        #[serde(default)]
        progress: f32,
        #[serde(default)]
        message: String,
    },
    Completed {
        timestamp: DateTime<Utc>,
    },
    Failed {
        error: String,
    },
}
```

用灰色、黄色、绿色、红色背景表示 NotStarted, InProgress, Completed, Failed 四种任务状态，statement、tutorial、std 状态显示到对应标签上；sols 中每个程序有自己的状态，在“解法”页分别显示，所有 sols 全绿时“解法”标签才变绿，否则如果有一个黄则标签为黄，否则如果有一个红则标签为红，否则标签为灰；generator、validator、checker、interactive_lib、std 这些程序有自己的任务状态，显示到辅助程序页中，它们全变绿时“辅助程序”标签才变绿，之后同“解法”标签。标签上除颜色外，额外显示一个环形进度指示器（进度环），直观反映该类别下子任务的完成百分比。鼠标指向红色内容会显示 error 字符串，指向任何状态内容均显示对应的 Tooltip 详情（如 InProgress 的 message、Completed 的时间戳）。

如果当前文件夹不是一个合法的比赛工程，则题目区只有一句提示“比赛工程未建立”和初始化比赛的按钮。

---

### 对话区

对话区是用户与 Agent 对话的区域，一个对话框，用户的信息靠右，agent 的信息（包括思维链、工具调用等）靠左，支持渲染 Markdown，下方输入框可以输入对话内容或“/”开头的系统命令，还有中止按钮用于强制中止所有 agent 活动，新建按钮用于新建 session。所有对话都是流式输出。Agent 的思维链内容默认折叠为 Spoiler，仅展示最终结论，用户可点击展开查看详细推理过程。

由于是多 agent 系统，每条信息上面还要写上 agent 的名称（就像聊天软件上的显示一样）

有一个下拉菜单可以选择 session。

未来会实现对话 fork 功能，现在还没有。

---

### 菜单栏

有比赛设置、API 设置、Agent 设置、导出按钮。比赛设置、API 设置、Agent 设置三个设置项均采用全屏遮罩层（Modal）或侧边抽屉（Drawer）形式展现，而非新开浏览器标签页，以便用户在修改配置时仍能参考主界面内容，修改后即时生效。

- 比赛设置参考数据结构部分
- API 设置可以为每个 agent 设置不同的供应商 base URL 和 API-key，以适应不同目的调用不同 LLM 的需要
- Agent 设置包括知识库、Skills，暂不支持增加新的工具调用，还可以设置每个 agent 的提示词
- 导出是一个下拉菜单，列出支持的格式

---

### 状态栏

有当前工程的路径、token 总消耗量及缓存命中率、（可选）估计花费，还有一个“比赛集成测试”按钮用于测试整个比赛。

---

前端需要监听本地文件系统变化，在本地文件被修改（用户手动或 agent 修改）后及时更新界面显示。

界面使用 Tauri，前端用 React 框架